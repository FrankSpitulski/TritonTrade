<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tmnt.tritontrade">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ForgotPassword" />
        <activity android:name=".Profile" />
        <activity android:name=".Welcome_Categories" />
        <activity android:name=".Create_Post" />
        <activity android:name=".Cart" />
        <activity android:name=".Settings" />
        <activity
            android:name=".Mainfeed"
            android:label="@string/title_activity_mainfeed"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".EditProfile" />
        <activity android:name=".categorySidebarPage"></activity>
    </application>

</manifest>